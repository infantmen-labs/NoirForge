{
  "version": 2,
  "framework": "docusaurus-2",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @noirforge/docs-site build && rm -rf build && cp -R packages/docs-site/build build",
  "outputDirectory": "build"
}
