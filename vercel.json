{
  "version": 2,
  "framework": "docusaurus-2",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @noirforge/docs-site build && if [ -d packages/docs-site ]; then mkdir -p build && cp -R packages/docs-site/build/. build/; fi",
  "outputDirectory": "build"
}
