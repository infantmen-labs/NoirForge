# rust-sdk-instruction-data

This example demonstrates building Solana verifier `instruction_data` using the Rust SDK:

`instruction_data = proof_bytes || public_witness_bytes`

## Run

```bash
cargo run --manifest-path examples/rust-sdk-instruction-data/Cargo.toml
```
